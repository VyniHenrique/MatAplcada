def soma (
        a : float,
          b : float = 5) -> float :
  """sum two numbers

  Args:
      a (float): first number
      b (float, optional): second number. Defaults to 5.

  Returns:
      float: return to sum number a and numbe b 
  """
    numero = a + b

    return numero